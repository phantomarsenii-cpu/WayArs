*.iml
.gradle
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/app/build

# Release signing keystore is decoded from a CI secret at build time — it
# must never be committed (see .github/workflows/build.yml).
/app/release.keystore
