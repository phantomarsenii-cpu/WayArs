<?xml version="1.0" encoding="utf-8"?>
<!--
TEMPORARY: android:packageNames removed again, on purpose, for exactly one
reason — finding out Bolt's and Wolt's REAL package names via the in-app
diagnostics panel (Settings -> Диагностика). They've never worked at all,
which strongly points to the guessed package names simply being wrong; with
this filter in place we'd never even see their events to find out. The
service-side code now bails out immediately (before any window scanning) for
every non-supported package, so the earlier "flooded with every app's
events" performance/stability concern is minimized: unsupported apps only
ever cost one cheap package-name check + a debounce, never a tree walk.
Once the real names are confirmed from that screen, put this filter back
with the corrected list.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRetrieveInteractiveWindows|flagReportViewIds|flagIncludeNotImportantViews"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="150"
    android:description="@string/accessibility_service_description" />
